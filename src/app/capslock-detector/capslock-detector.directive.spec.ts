import { CapslockDetectorDirective } from './capslock-detector.directive';

describe('CapslockDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new CapslockDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
